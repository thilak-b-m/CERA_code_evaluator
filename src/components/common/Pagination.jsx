export default function Pagination({ page = 1, total = 1, onChange }) { return <div style={{ display: 'flex', gap: 8, alignItems: 'center', justifyContent: 'flex-end', padding: 14 }}><button className="btn btn-ghost" disabled={page <= 1} onClick={() => onChange?.(page - 1)}>Previous</button><span style={{ color: 'var(--muted)', fontSize: 11 }}>Page {page} of {total}</span><button className="btn btn-ghost" disabled={page >= total} onClick={() => onChange?.(page + 1)}>Next</button></div>; }